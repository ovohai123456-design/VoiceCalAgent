spring:
  application:
    name: voicecal-agent

  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}

  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:62.234.45.155:8848}
      config:
        file-extension: yaml
        shared-configs:
          - data-id: share.yml
            group: DEFAULT_GROUP
            refresh: true
