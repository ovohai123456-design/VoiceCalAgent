skill_id: meeting.create
name: Create Tencent Meeting
description: Create a Tencent Meeting number and join link before a calendar event is written
category: meeting
enabled: true
trigger_examples:
  - create online meeting
  - meeting link
  - 线上会议
  - 会议链接
  - 腾讯会议
input_schema:
  type: object
  required:
    - title
    - start_time
    - end_time
output_schema:
  type: object
  required:
    - code
    - url
executor:
  type: mock
  mock_name: meeting.create
