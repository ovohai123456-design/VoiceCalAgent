skill_id: calendar.delete
name: Delete calendar event
description: Delete a selected calendar event
category: calendar
enabled: true
trigger_examples:
  - delete calendar event
  - 删除日程
  - 取消日程
input_schema:
  type: object
  required:
    - event_id
    - user_id
output_schema:
  type: object
executor:
  type: native
  tool_key: calendar.delete
